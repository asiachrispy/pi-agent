---
id: D-2026-004
title: "team 流程效率改进：subagent 隔离 + summary 缓存 + demand metadata + release protocol + 决策表"
weight: standard
status: done
outcome: delivered
step: verify
owner: asiachrispy
assignee: ""
project: root
repos:
  - pi-agent
risk: low
prd_ref: ""
prototype_ref: ""
exec_spec_ref: ""
created_at: "2026-06-16T12:30:00+08:00"
updated_at: "2026-06-16T12:55:00+08:00"
---

# D-2026-004 team 流程效率改进

## 意图

承接 D-2026-002 / D-2026-003 release 流程的复盘：每个 /team 都有 ~10 个"需人决策"点 + 反复读 5+ wiki + 反复 git status / tsc / vitest + 反复写回 demand，token 与时间都浪费在重复劳动上。落地 5 项改进。

## 契约

- 范围：仅改 `pi-agent` 工作区（不动 pi / pi-app），覆盖：
  1. 修复 subagent team 注册，让 /team 真有 L2 隔离
  2. 写 `wiki/summary.md`：工作区一次性摘要，替代每次读 5+ wiki
  3. 需求级 metadata 字段（`requires_release/requires_upstream/requires_gh_release/cold_smoke_required/visual_qa_required`）写到 `demands/template.md`，让决策前置
  4. 写 `.pi/protocols/release.md` release protocol 模板
  5. 决策表（weight fast-path / 越界默认 A1 / upstream==HEAD 不 merge / cold-smoke 动态端口）落到代码或文档
- 不做：
  - 不改 pi / pi-app
  - 不发版（无需 package:macos / DMG / gh release）
  - 不改现有 demand（D-2026-002 / D-2026-003 已 done）

## 交付

### 1. subagent 注册修复（partial）

- 创建 `~/.pi/agent/agents/team.md`（user-level 副本，因为该目录原本不存在）
- 创建 `~/.pi/agent/bin/pi` shim：让 `pi` CLI 走 `node <bundled cli.js>` 调用（在 pi-app 会话中 default `command: "pi"` 能找到 bundled CLI）
- 验证：直接 `pi --mode json -p --no-session "Task: echo hi"` 正常输出 JSON；用 `node child_process.spawn("pi", ...)` 也正常 exit 0 + agent_end
- **未完全修复**：subagent 工具在当前 pi-app 会话调用仍返回 "Agent failed: (no output)"。已确认 spawn + child output 正常，问题在 MCP/渲染层（pi 引擎代码），跨仓修复
- **降级方案**：team 在主会话直接执行（已是当前模式）；后续若修好 subagent 渲染可恢复 L2 隔离

### 2. wiki/summary.md（完成）

- 文件：`wiki/summary.md`（6.1 KB）
- 包含：业务仓元数据（snapshot 动态填入）+ 决策树 + skill 触发矩阵 + 关键命令（按场景分组）+ 子目录速查 + 已知陷阱
- 配套脚本：`scripts/snapshot-workspace.sh` + `scripts/_snapshot_block.py`（自动刷新 snapshot 段）
- 当前 snapshot：pi HEAD `5a2b03e` / pi-app HEAD `f62e2c4` / pi-app tag `v0.8.6` / pi-app upstream `a7c5de3`
- 独特标记 `>>>SNAPSHOT_START/END<<<` 避免 markdown 文本里字面 `<!-- snapshot-start -->` 冲突

### 3. demands/template.md metadata 字段（完成）

- 新增字段：
  ```yaml
  requires_release: false
  requires_upstream_sync: false
  requires_gh_release: false
  cold_smoke_required: true
  visual_qa_required: false
  release_scope: []  # 合法值: commit|verify|version|push|tag|package|dmg|cold_smoke|gh_release
  ```
- 用途：用户创建 demand 时即填好，team 据此自动决定 release 流程范围，**不再中途询问**
- `release_scope` 一次性授权发版步骤：用户在 `/team D-xxx release` 时显式给出，team 按 scope 执行不重复确认

### 4. .pi/protocols/release.md（完成）

- 文件：`.pi/protocols/release.md`（3.5 KB）
- 9 步标准流程 + 3 步 human_confirmation（5 push / 6 push tag / 9 gh release）+ 自动化决策点
- 失败模式矩阵（步骤 1-4 / 5 push / 7 package / 8 冷烟 / 9 gh release 各自的处理）
- 触发判定 + 复盘规范

### 5. wiki/decisions/team-decisions.md（完成）

- 文件：`wiki/decisions/team-decisions.md`（4.6 KB）
- 12 类决策点：weight 判定 / 越界 diff / upstream 同步 / 冷烟端口 / 拆 commit / lockfile / DMG 基线 / push&tag&gh release / status 转换 / 询问判定 / 反模式 / 改进流程
- **核心目标**：把"反复问的同款问题"沉淀到表里，team 读表自决，不询问

## 验证

- `bash scripts/validate-demand.sh demands/D-2026-004.md` → `ok: demands/D-2026-004.md`
- `bash scripts/check-pi-env.sh` → 0 errors, 2 warnings（warnings = `node`/`npm` 不在默认 PATH，与本改动无关；用户已通过 `/opt/homebrew/bin` 临时 export 解决）
- `bash scripts/snapshot-workspace.sh` → 成功填入 snapshot 块
- `ls` 5 个新文件全部存在
- `~/.pi/agent/bin/pi` shim 可用（直接调用 exit 0）

## 阻塞

（无）

## 批准

- 2026-06-16 12:55 / 批准人：用户（“全部执行”） / 事项：5 项改进全部落地，#1 subagent partial 修复（跨 pi 引擎层渲染问题降级主会话），其余 4 项完整可用。

## workflow_update

```yaml
workflow_update:
  demand: D-2026-004
  status: done
  outcome: delivered
  step: verify
  weight: standard
  owner: asiachrispy
  assignee: ""
  handoff: ""
  blocking_reason: ""
  verification:
    scope: targeted
    commands:
      - "bash scripts/validate-demand.sh demands/D-2026-004.md"
      - "bash scripts/check-pi-env.sh"
      - "bash scripts/snapshot-workspace.sh"
      - "ls wiki/summary.md wiki/decisions/team-decisions.md .pi/protocols/release.md"
      - "ls scripts/snapshot-workspace.sh scripts/_snapshot_block.py"
      - "ls ~/.pi/agent/bin/pi"
    result: pass
  next_action: "无需续跑。下次 /team 启动读 wiki/summary.md（应减少 ~50% token 消耗）。"
  human_confirmation_required: false
```
