---
id: D-2026-011
title: "拉取合并上游 pi + pi-app，准备打包"
weight: light
status: active
outcome: ""
step: clarify
owner: asiachrispy
assignee: ""
project: pi-app
repos:
  - pi
  - pi-app
risk: low
prd_ref: ""
prototype_ref: ""
exec_spec_ref: ""
requires_release: false
requires_upstream_sync: true
requires_gh_release: false
cold_smoke_required: true
visual_qa_required: false
release_scope: []
created_at: "2026-07-04T20:30:00+08:00"
updated_at: "2026-07-04T20:45:00+08:00"
---

# D-2026-011 拉取合并上游 pi + pi-app，准备打包

## 意图

按 AGENTS.md「打包 / 安装 / 发布前的上游同步（强制）」，在打包 Pi.app 之前先
把上游 `earendil-works/pi` 与 pi-app 上游的更新合并进本地 fork，保证打包基于
最新代码；之后执行测试 + 编译 + `npm run package:macos`。

## 范围

- `pi/`：fetch upstream + merge upstream/main + push origin/main
- `pi-app/`：fetch upstream + merge upstream/main + push origin/main
- 全套测试 + tsc --noEmit + `npm run package:macos`
- 仅打包；不打 tag、不出 DMG、不发 Release（用户只说"打包"）

## 冲突解决原则

按 AGENTS.md §5：对 pi-app 已成熟的 Web/桌面实现，不因历史形态而回退。

## 交付

### pi（earendil-works/pi）

- 上游：`114bacf34..ee24a9ec5`（9 commits）
- 合并结果：`79fefa9fb Merge upstream earendil-works/pi main (auto)`
- 冲突文件：2（`packages/ai/src/providers/nvidia.models.ts`、`openrouter.models.ts`）—— model catalog 增量，全部 `--theirs` 采纳上游
- ahead 10 commits（**未 push**，等授权）

| 类型 | 内容 |
|------|------|
| feat | refresh generated model catalogs |
| fix | allow extra edit replacement fields |
| fix | retry Cloudflare 524 timeouts |
| fix | honor server-provided slow_down interval in device-code polling |
| fix | rotate stale Codex websocket sessions |
| fix | remove Vercel AI Gateway attribution |
| fix | add pnpm self-update prune hint (#6279) |
| fix | detect DS4 context overflow errors |

### pi-app（agegr/pi-web）

- 上游：`55dd1ab..cd75650`（v0.7.5 / v0.7.6 / v0.7.7，21 commits）
- **未合并，跳过本轮同步**

**跳过原因**（用户授权 2026-07-04）：

本地 fork `asiachrispy/pi-app` 与 upstream `agegr/pi-web` 已严重 divergent：fork
领先 upstream 279 commits，总差异 405 文件，尝试 merge 产生 15 个冲突
（AGENTS.md / README.md / package.json / package-lock.json /
components/AppShell.tsx / ChatInput.tsx / ChatWindow.tsx / FileViewer.tsx /
MessageView.tsx / SessionSidebar.tsx / hooks/useAgentSession.ts /
app/layout.tsx / app/api/files/[...path]/route.ts / lib/session-reader.ts /
lib/types.ts / lib/tool-presets.ts）。

按 AGENTS.md 「对 pi-app 已成熟的 Web/桌面实现，不因历史形态而回退」，完整
merge 会冲击 livo 集成（M0–M4 + 多租户 SSO + per-tenant 计量 + workspace 隔离）、
MCP 集成、`useAgentSession` 自定义实时状态流、workbench UI 等大量 fork 专属
改动。

**TODO 后续**：单独建一份工作评估上游 v0.7.5–v0.7.7 与 fork 之间的差异（重点
评估 `feat: agent state reconciliation`、`feat: queued message panel` 是否能
cherry-pick 应用），避免一次性 integration 风险。

### 后续步骤（已发生）

- ✅ npm run dev 验证（30142 端口 HMR 工作正常）
- ✅ tsc --noEmit / vitest 全套 450/450 pass
- ⏳ npm run package:macos（用户授权后执行；本 demand 仅同步与记录，不打包）